# Outbound webhooks (Slack incoming webhook URL, Microsoft Power Automate, generic HTTPS POST).
# Payload shape: { "event": "<name>", "occurred_at": "<ISO8601 UTC>", "data": { ... } }
# Events: use_case_approved | mapping_changed | audit_completed
enabled: false
endpoints: []
# Example:
# endpoints:
#   - url: "https://hooks.slack.com/services/XXX/YYY/ZZZ"
#     events:
#       - use_case_approved
#       - mapping_changed
#       - audit_completed
